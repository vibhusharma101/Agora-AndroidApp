<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.aossie.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppThemeB">
        <activity android:name=".ui.HomeActivity"></activity>
        <activity android:name=".ui.profile.UpdateProfileActivity" />
        <activity android:name=".ui.profile.ProfileActivity" />
        <activity android:name=".ui.InviteVotersActivity" />
        <activity android:name=".ui.ElectionDetailsActivity" />
        <activity android:name=".ui.createElection.CreateElection">
        </activity>
        <activity android:name=".ui.ForgotPasswordActivity" />
        <activity android:name=".ui.TwoStepAuthActivity" />
        <activity android:name=".ui.SignUpActivity" />
        <activity android:name=".ui.LoginActivity" />
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>